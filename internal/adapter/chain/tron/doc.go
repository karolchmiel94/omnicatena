// Package tron implements the chain ports for TRON (TVM, base58 addresses,
// energy/bandwidth resource model). Uses the TRON full node HTTP JSON API directly.
// Local dev uses tron-quickstart; fall back to Nile/Shasta testnet if the local
// node is troublesome (ADR-0003).
package tron
